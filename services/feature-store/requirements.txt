# meridian runtime deps — pinned for reproducible installs (assurance-r3).
# Internal package: CI installs from the repo root (PYTHONPATH +
# pip install -r services/<svc>/requirements.txt), so this relative path
# resolves against the repo root. Local dev equivalent:
#   pip install -e packages/events/python
meridian-events @ file:packages/events/python
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
httpx==0.28.1
pytest==9.1.1
# HARDENING H1 prod profile:
PyJWT[crypto]==2.13.0  # AUTH_MODE=keycloak (RS256/JWKS)
psycopg[binary]==3.3.4 # DATABASE_URL Postgres
duckdb==1.5.5 ; python_version >= "3.11"
